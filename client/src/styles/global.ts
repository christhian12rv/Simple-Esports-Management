import cleanGrayWave from '../images/cleanGrayWave.jpg';

export default {
	body: {
		backgroundImage: `url(${cleanGrayWave})`,
		margin: 0,
		padding: 0,
		outline: 0,
		fontFamily: ['Roboto', 'sans-serif'].join(','),
	},
};